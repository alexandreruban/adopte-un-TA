<div class="banner" style="background-image: linear-gradient(-225deg, rgba(200,0,100,0.6) 0%, rgba(20,18,61,0.6) 50%), url('<%= image_path 'banner.jpg' %>');">

  <div class="banner-content">
    <h1>Adopt a TA</h1>
    <p>Learn to code with a TA</p>
    <%= link_to "All courses", courses_path, class: "btn red-button lg" %>
  </div>
</div>

<div class="container">
  <div class="row">
    <% @courses.each do |course| %>
    <div class="col-xs-12 col-sm-4">
      <%= render partial: 'shared/card', locals: {
        course: course } %>
    </div>
    <% end %>
  </div>
</div>
